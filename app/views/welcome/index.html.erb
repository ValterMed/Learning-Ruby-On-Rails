<p>Index of main page</p>

<% @articles.each do |article| %>
  <div>
    <h1 class="text-center"><%= link_to article.title, article %></h1>
    <p><%= article.body %></p>
    <p class="text-center"> author: Walter </p>
  </div>
<% end %>