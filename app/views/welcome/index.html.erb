<p>Index of main page</p>

<% @articles.each do |article| %>
  <div class="jumbotron color-a">
    <h1 class="text-center"><%= link_to article.title, article %></h1>
    <p class="text-center color-a-text"><%= article.body %></p>
  </div>
<% end %>