<% @articles.each do |article| %>
  <div>
    <h1><%= article.title %></h1>
    <p><%= article.body %></p>
    <%= link_to "Edit", edit_article_path(article) %>
    <%= link_to "Delete", article, method: :delete, data: {confirm: "Are you sure to delete this article?"} %>
  </div>
<% end %>
